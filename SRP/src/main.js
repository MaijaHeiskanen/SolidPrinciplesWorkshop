import { Pizzeria } from "./Pizzeria.js"; // Import Pizzeria to see errors. If you don't care if your code is actually working, remove the import and just do pseudo code or smth.

console.log("All working, your app is running!");

// new Pizzeria().takeAnOrder("email@email.com", "Margarita", ["Salami"]).catch((err) => console.error(err));
