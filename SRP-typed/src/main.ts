import { Pizzeria } from "./Pizzeria"; // Import Pizzeria to see errors. If you don't care if your code is actually working, remove the import and just do pseudo code or smth.

console.log("All working, your app is running!");

new Pizzeria();
